const filter = () => {
    const menu = document.querySelector('.portfolio-menu'),
         items = menu.querySelector('li'),
         btnAll = menu.querySelector('.all'), 
         btnLovers = menu.querySelector('.lovers'), 
         btnChef = menu.querySelector('.chef'), 
         btnAll = menu.querySelector('.all'), 
         btnAll = menu.querySelector('.all'), 
         btnAll = menu.querySelector('.all'), 
         btnAll = menu.querySelector('.all'), 
         btnAll = menu.querySelector('.all'), 


}

export default filter;